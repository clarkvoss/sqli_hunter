# Header SQLi Hunter — Python dependency (library, not the ProjectDiscovery httpx CLI)
httpx>=0.27

# Optional external tools for the harvesting stage are NOT pip packages.
# Install separately and put them on PATH:
#   waybackurls  github.com/tomnomnom/waybackurls
#   gau          github.com/lc/gau
#   katana       github.com/projectdiscovery/katana
#   gospider     github.com/jaeles-project/gospider
#   hakrawler    github.com/hakluke/hakrawler

# Optional: needed only for --respect-hours if your Python lacks system tz data
#   pip install tzdata
